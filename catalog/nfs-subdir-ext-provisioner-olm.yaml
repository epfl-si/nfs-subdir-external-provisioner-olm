---
defaultChannel: preview
description: |
  # NFS subdir external provisioner OLM operator
name: nfs-subdir-external-provisioner-olm
schema: olm.package

---
schema: olm.channel
package: nfs-subdir-external-provisioner-olm
name: preview
entries:
  - name: nfs-subdir-external-provisioner-olm.v@@VERSION@@
